mixin form-change-multi(path)
    form(action=path, method="POST", form-change-multi)
        .card
            .input-group
                .input-group-prepend.dropdown(class="bootstrap-select default-select form-control wide bleft")
                    select(name="type", class="default-select form-control wide bleft")
                        option(disable selected) Chọn hành động...
                        option(value='active') Hoạt động
                        option(value='inactive') Dừng hoạt động
                        option(value='delete-all') Xoá tất cả
                        option(value='change-position') Thay đổi vị trí

                input(type="text", name="ids", class="form-control d-none")

                .input-group-append
                    button.btn.btn-primary(type='submit') THAY ĐỔI